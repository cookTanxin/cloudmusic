import React, { memo } from "react";
// style
import { AppFooter } from "./style";
const index = memo(() => {
  return (
    <AppFooter>
      <div>页脚部分</div>
    </AppFooter>
  );
});

export default index;
